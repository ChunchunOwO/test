[h1]ECHO 创意工坊使用指南[/h1]

这篇指南面向订阅和使用创意工坊内容的普通用户。你可以在 ECHO 中使用主题、歌词场景、可视化、DSP / EQ 预设、VST3 / VST3i 接入配置和沙箱插件。订阅只负责让 Steam 下载内容；ECHO 不会因为订阅而自动执行代码、切换界面或修改音频设置。

[h1]30 秒快速上手[/h1]

[olist]
[*]在 Steam 创意工坊页面选择一个项目并点击“订阅”。
[*]等待 Steam 下载完成，然后启动或返回 ECHO。
[*]打开“创意工坊 → 已安装”。
[*]查看项目类型、版本、作者、权限和兼容性。
[*]点击“使用”；如果是插件，阅读并确认精确权限清单。
[*]检查实际效果。需要恢复时，先停用项目或切回内置主题。
[/olist]

订阅、下载、导入、启用和应用是不同步骤。“Steam 已订阅”不代表项目已经在 ECHO 中运行。

[h1]支持的内容[/h1]

[list]
[*][b]主题[/b]：调整颜色、控件和页面外观，也可以提供隔离的完整自定义 UI。
[*][b]歌词场景[/b]：改变歌词、翻译、封面、进度和频谱的布局与视觉；启用后可在歌词设置中直接选择或切回 ECHO 内置布局。
[*][b]可视化预设[/b]：配置条形、波形、环形视觉及灵敏度、平滑和衰减。
[*][b]DSP / EQ 预设[/b]：提供经过校验的均衡器参数；应用后可能改变音频信号。
[*][b]VST3 / VST3i 接入配置[/b]：描述你已从厂商正规安装的本机插件、参数映射和预设；工坊项目不包含插件二进制。
[*][b]沙箱插件[/b]：增加命令、工具面板、可搜索音源、可选择的歌词源、曲库整理、播放列表、队列工具、直链音源、作者 Agent 或一起听工作流。
[/list]

[h1]每种内容具体怎么用[/h1]

[h2]主题[/h2]

[olist]
[*]在“已安装”找到主题，点击“使用”或“启用并切换”。
[*]等项目显示“当前主题”，再检查侧栏、播放器、卡片、颜色和页面舞台。
[*]如果主题包含完整自定义 UI，第一次使用时确认来源和声明能力。
[*]想恢复时，到 ECHO 主题设置选择一个内置主题，再停用或退订工坊主题。
[/olist]

[h2]歌词场景与歌词主题[/h2]

[olist]
[*]点击“使用”，然后播放一首确实有歌词的歌曲并进入歌词页。
[*]打开歌词设置，在“创意工坊歌词主题”中选择已启用的场景。
[*]检查歌词、翻译、封面、进度、频谱和动效布局。
[*]选择“ECHO 内置”即可退出当前工坊歌词主题。
[/olist]

[h2]可视化预设[/h2]

[olist]
[*]点击“使用”，打开 ECHO 可视化页面并开始播放音乐。
[*]检查颜色、条形/波形/环形样式、灵敏度、平滑和衰减。
[*]暂停、静音或歌曲能量较低时，画面变化可能不明显。
[/olist]

[h2]DSP / EQ 预设[/h2]

[olist]
[*]先降低音量，再点击“应用到 DSP”。
[*]播放一首熟悉的歌曲，在 DSP / EQ 控制面确认参数已经切换。
[*]如果响度或音色异常，立即降低音量并切回内置预设或手动设置。
[/olist]

“已启用”只表示预设可以使用；看到 Audio Core / DSP 控制面报告对应状态，才表示参数已经进入真实音频链路。

[h2]VST3 / VST3i 接入配置[/h2]

[olist]
[*]查看项目写明的厂商、插件名称、Class ID、效果器/音源角色和适配器最低版本。
[*]从厂商正规渠道自行安装并激活完全匹配的本机插件。
[*]按项目要求准备兼容的可选 ECHO Audio Core 适配器。
[*]回到“已安装”点击“启用接入配置”。
[*]以 Audio Core 报告的实例、旁路、延迟和故障状态判断是否真实生效。
[/olist]

“配置已启用”不等于“插件已加载”，也不等于声音已经经过处理。工坊包不应包含 DLL、VST3 bundle、安装器、序列号或破解文件。

[h2]沙箱插件[/h2]

[olist]
[*]点击“使用”，逐项阅读并确认权限。第一次启用和权限变化后都会重新询问。
[*]启用后点击 ECHO 界面右下角“插件”入口。
[*]命令：点击后执行一次作者定义的操作。
[*]面板：在 ECHO 宿主持有的窗口中打开作者 UI；关闭按钮始终由 ECHO 控制。
[*]Agent：输入任务并点击“运行 Agent”；它只能使用同一插件已经获批的能力。
[*]可搜索音源：打开提供器、输入搜索词、选择结果并播放；首次使用某个域名时核对来源确认框。
[*]插件设置：从“插件 → 插件设置”调整作者声明的文本、数字、开关和选项，不要填写密码、Cookie、Token 或 API Key。
[*]歌词源：播放歌曲后进入歌词设置，在“创意工坊歌词源”选择来源、查询候选，再点击“应用”。
[*]一起听：每次上传当前本地歌曲前核对歌曲与目标服务；没有保存期限、删除方法和隐私说明时不要允许上传。
[/olist]

如果插件入口没有出现，先确认项目显示“已启用”，等待几秒后刷新。如果功能长期显示“正在加载”，停用项目并记录项目号、版本和错误信息。停用会移除命令、面板、Agent、音源和事件监听，但不会自动取消 Steam 订阅。

[h1]插件权限怎么看[/h1]

插件第一次启用，以及更新后权限清单发生变化时，ECHO 会再次要求确认。只批准你理解并愿意使用的能力。

[list]
[*][b]playback:read[/b]：读取播放状态，不返回本地文件路径。
[*][b]playback:control[/b]：播放、暂停、seek、上一首和下一首。
[*][b]playback:share[/b]：把当前本地歌曲交给一起听服务。启用时会列出上传/CDN 域名；每次真正上传前还会再次显示歌曲和目标域名。
[*][b]library:read[/b]：读取经过清理的曲目、专辑、艺人、流派、收藏和本地播放列表。
[*][b]library:control[/b]：修改收藏，创建本地播放列表并添加曲目。
[*][b]queue:read / queue:control[/b]：查看或编排当前播放队列。
[*][b]audio:spectrum[/b]：读取 Audio Core 提供的频谱和能量数据，用于可视化。
[*][b]sources:provide[/b]：在 ECHO 宿主界面注册可搜索音源目录。插件返回候选和直接播放 URL；ECHO 校验结果、显示来源确认并持有队列和实际播放。
[*][b]sources:direct[/b]：请求 ECHO 播放 HTTP(S) 直链音源；当前会话首次使用某个域名时会再次确认。
[*][b]network:request[/b]：通过宿主向清单列出的固定域名发送受限 GET/POST 请求，可用于远程歌词目录、网络音源目录、Webhook 或作者自有 Agent 服务；不携带 Cookie、Steam 身份或本地路径。
[*][b]agent:runtime[/b]：运行作者编写的 Agent 逻辑。ECHO 不替作者提供统一模型或 API Key。
[*][b]lyrics:provide[/b]：在歌词设置中提供由作者编写的歌词源；只接收当前歌曲的清理后元数据。
[*][b]fs:plugin[/b]：保存少量插件私有状态，不是任意文件系统权限。
[/list]

沙箱插件不会得到 Node、Electron、Steamworks、DLL、任意文件、原始 IPC、Cookie 或 native host。停用项目后，其命令、面板和事件监听会从运行时移除。

插件可以声明自己的文本、数字、开关和选项设置。ECHO 会在宿主拥有的“插件设置”页面统一显示、校验并按插件隔离保存；这些值不会进入 ECHO 全局设置。Workshop 不接受密码或 API Key 类型的设置，请不要把凭据填入普通文本项。

[h1]选择歌词源与歌词主题[/h1]

申请 lyrics:provide 的插件会出现在歌词设置的“创意工坊歌词源”中。只有你选择某个源并点击查询时，ECHO 才会把当前歌曲的标题、艺人、专辑和时长交给它，不会提供本地路径或音频内容。返回歌词先显示为候选；你再次点击“应用”后才会替换当前歌词。

启用 lyrics-style 项目后，它会出现在“创意工坊歌词主题”中。你可以在多个已启用场景间切换，也可以选择“ECHO 内置”关闭当前 Workshop 场景。歌词场景是经过校验的声明式布局，不会运行作者 HTML、JavaScript 或任意 CSS。

lyrics:provide 本身不包含任意网络、Cookie、平台登录或网页抓取。来源不明、要求账号凭据或没有说明歌词权利的项目不要使用。

[h1]一起听与本地歌曲分享[/h1]

`sources:direct` 只能播放已经存在的网络音频 URL，不能代替本地歌曲串流。一起听插件需要申请 `playback:share`：

[olist]
[*]插件读取一份不含路径的当前歌曲摘要。
[*]你确认本次上传的歌曲和目标服务。
[*]ECHO 主进程把本地文件流式上传到项目清单声明的域名。
[*]插件只能查看任务进度和服务返回的播放 URL。
[*]播放 URL 再交给 ECHO 原有队列与 Audio Core。
[/olist]

上传意味着音频会离开你的电脑。请只使用可信服务，只分享你有权传输的内容，并先阅读作者说明的保存时间、删除方法和隐私规则。如果项目没有写清这些信息，不要批准上传。

[h1]直链音源与网络内容[/h1]

直链音源用于用户明确提供或作者合法运营的 HTTP(S) 音频流，例如网络电台或自有服务器。插件若申请 [b]network:request[/b]，还可以访问启用确认框列出的固定 API 域名来查询合法目录或调用自有服务。该连接只有受限 GET/POST，并限制大小、超时、跳转和并发；不会获得浏览器 Cookie、Steam 身份、本地路径或自动凭据，也不能用来绕过第三方平台限制。

确认来源时请核对显示的域名。来源异常、突然跳转到未声明域名或需要登录 Cookie 时，应拒绝并向作者反馈。

[h1]作者 Agent[/h1]

Workshop Agent 是插件作者在沙箱中编写的规则、算法和交互，可以把曲库查询、收藏、播放列表、队列或页面导航组合成新工具。Agent 只能使用该插件已经申请且由你批准的能力；它不会自动获得更多权限。

如果作者选择接入外部模型或自有服务，应在项目说明中写明服务方、发送的数据、关闭方式和隐私规则。不要把密码、Cookie、Steam Guard、许可证密钥或其他秘密输入不可信的 Agent。

[h1]VST3 / VST3i 接入配置[/h1]

这类项目只分享 Class ID、参数映射、便携预设和路由建议。使用前：

[olist]
[*]从插件厂商正规渠道安装并激活匹配的 VST3 / VST3i。
[*]确认项目显示的 Class ID、厂商和插件名称与你本机版本一致。
[*]按项目要求安装兼容的可选 ECHO Audio Core 适配器。
[*]在“已安装”页面启用接入配置。
[*]以 Audio Core 报告的实例、旁路、延迟和故障状态判断是否真实生效。
[/olist]

“配置已启用”不代表插件已加载，更不代表声音一定经过处理。工坊包不应该包含 .dll、.vst3、安装器、序列号、破解补丁或下载脚本。

[h1]完整自定义 UI 与紧急退出[/h1]

完整 UI 主题可以改变可见界面结构，但仍运行在隔离 frame 中。ECHO 会保留主题无法遮盖的退出控制；任何时候都可以按 [b]Ctrl+Shift+F12[/b] 退出当前自定义 UI 会话。

第一次启用含 UI runtime 的主题时，应确认项目来源和声明能力。出现空白、无法操作或布局异常时，先使用紧急退出，再停用主题并向作者报告 ECHO 版本和项目版本。

[h1]更新、停用与退订[/h1]

[b]更新[/b]：Steam 下载新版本后，ECHO 会重新校验文件清单、哈希、兼容性和权限。新增或变化的权限必须重新确认。

[b]停用[/b]：停止在 ECHO 中使用项目，但不会取消 Steam 订阅或删除 Steam 文件。

[b]退订[/b]：取消 Steam 后续下载。ECHO 可能仍保留安全登记，直到刷新或安全修复完成。

建议更新后先查看版本与权限，再点击“更新并继续使用”。表现异常时先停用并切回内置内容，不要手工修改清单或绕过校验。

[h1]状态与故障排查[/h1]

[list]
[*][b]等待下载[/b]：Steam 尚未提供完整本地文件。检查 Steam 下载队列并刷新。
[*][b]等待导入[/b]：文件已下载但尚未进入 ECHO 安全登记。点击“使用”或“同步已安装版本”。
[*][b]待确认[/b]：项目需要你确认权限或高影响操作。
[*][b]已启用[/b]：项目已进入运行时；仍需按内容类型检查实际效果。
[*][b]有更新[/b]：存在较新版本，更新后会重新校验。
[*][b]已隔离[/b]：清单、哈希、文件类型、兼容性或权限校验失败。不要强制放行。
[*][b]只读保护[/b]：登记目录或文件暂时不能安全写入。重启 ECHO，仍未恢复再运行“安全修复”。
[/list]

“安全修复”会核对 Steam 订阅、已安装内容、ECHO 登记与可用目录之间的关系；它不会篡改作者文件，也不会绕过失败校验。

[h1]反馈问题时提供什么[/h1]

向作者反馈时，请提供项目号、ECHO 版本、项目版本、页面状态、操作步骤和错误码。不要公开完整本地路径、歌曲文件名、Token、Cookie、许可证、序列号、Steam 票据或个人日志。

如果项目涉及侵权、欺诈、恶意内容或没有说明的数据上传，请停止使用，并通过 Steam 项目页提供的举报入口处理。

[h1]最后提醒[/h1]

创意工坊的价值是让作者创造 ECHO 本体没有预先想到的体验。开放能力不等于自动信任：看清项目来源和权限，遇到网络上传时再次确认，出现问题时能够随时停用、恢复和反馈，才是长期使用创意工坊最稳妥的方式。
